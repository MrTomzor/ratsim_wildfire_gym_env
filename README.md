# ratsim_wildfire_gym_env
# TODO - this whole repo and package should later be renamed to ratsim_gym_env, with one core env that can be configured to different worlds, agent setups and reward object spawn distribution (using the unified config generation in ratsim)
